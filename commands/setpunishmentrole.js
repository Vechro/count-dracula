const jsonfile = require("jsonfile");
const { path } = require("../config.json");

module.exports = {
    name: "setpunishmentrole",
    description: "Designate a role to be given when.",
    aliases: ["startat"],
    execute(message, args) {

    },
};